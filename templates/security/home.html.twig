{% extends 'base.html.twig' %}

{% block title %}Acceuil{% endblock %}

{% block body %}

    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey | trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div id="carouselExampleFade" class="carousel slide carousel-fade" data-interval="5000" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100" height="700px" src="../assets/images/Hero.jpg" alt="First slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" height="700px" src="../assets/images/Radiant.jpg" alt="Second slide">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleFade" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleFade" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>

    <div class="container-fluid">
        <div class="row justify-content-center">
            <h4>Bienvenue sur Manka-K, site de référencement de mangas et d'animés</h4>
            <h6>(Ce site est actuellement en phase de développement, il est donc susceptible de changer à tout moment.)</h6>
        </div>
        <div class="row bg-light text-dark p-5" id="manga_home">
            <p class="col-12 pb-4">Pour jeter un coup d'oeil aux mangas actuellement référencés, kochi kochi.</p>
            <button type="button" class="btn btn-outline-dark btn-block col-2 offset-1 clignote">Mangas</button>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row bg-dark text-light p-5" align="right" id="manga_home">
            <div class="col-12">
                <p class="pb-4">Pour les animes actuellement référencés, kochi ni.</p>
                <button onclick="location.href='{{ path("animes") }}'" type="button" class="btn btn-outline-light btn-block col-2 offset-9 clignote">Animes</button>
            </div>
        </div>
    </div>
{% endblock %}
